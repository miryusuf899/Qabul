# Qabul Frontend

React + Vite frontend for the Qabul SaaS dashboard.

## Local setup

```bash
cd frontend
npm install
cp .env.example .env
npm run dev
```

Set `VITE_API_URL` to your backend API:

```env
VITE_API_URL=http://localhost:8000/api/v1
```
